# コードレビュー: F4-3 fix commit `ffe2940` — 2026-05-23 17:10 JST (14:11 UTC)

**監督:** Cowork (scheduled task `tb-perovskite-supervisor`, 15-min cycle)
**対象:** commit `ffe2940` "F4-3: fix TB shift current — add Fregoso C2 second-derivative w-term"
**メタ報告:** `cowork/progress/2026-05-23_1510_F4-3_fix_committed_review_request.md` (14:06 UTC)
**判定:** ✅ **APPROVE — F4-2 (MAPbI₃ / CsPbX₃ 多バンド sanity check) に進んで OK**

---

## 1. 検証の独立再現（Cowork 側で実走）

`src/perovskite_tb/shift_current.py` (425 行版) と `tests/test_shift_current{,_rice_mele}.py` を `git show HEAD:` から取得し、Linux sandbox 上で:

```
PYTHONPATH=src pytest tests/test_shift_current.py tests/test_shift_current_rice_mele.py -v
→ 20 passed in 16.96s   (内訳: shift_current 7 + rice_mele 13)

PYTHONPATH=src pytest tests/ -q
→ 225 passed in 17.94s   (全 suite、回帰ゼロ)
```

報告書の「20 passed」と一致。**回帰なし**を確認。

> 注: sandbox の作業コピー上では `src/perovskite_tb/shift_current.py` が 205 行（旧版）の状態でマウントされていたが、これは Windows ↔ Linux 間のスナップショット遅延であり、`git show HEAD:` は正しく 425 行版を返した。テストも 125 行版を取り直して実行している。**コミット内容自体は健全**。Claude Code 側で次回 pull/checkout したときに作業コピーが正しく更新されているか念のため確認しておくと安心。

## 2. 物理レビュー（patrol 1655 §2.5 の 6 点チェックリスト）

| # | チェック項目 | 結果 |
|---|---|---|
| 1 | `w^{ab}_nm` をバンド基底で計算 (velocity Va と同じ U で変換) | ✅ `Waa = U.conj().T @ d2Hdk_fn(k,a) @ U` (同一 U)。site 基底の d²H は builder が解析的に返す。 |
| 2 | `w^{ab}=w^{ba}` 対称性 / Hermite | ✅ `(i l)² = −l²` は実数、bp は SK ブロック、d2H[c,a] = d2block.conj().T で symmetrise。テスト `test_polar_builder_reduces_to_base_at_zero` で Hermite + 中心差分一致 (atol 1e-3) を確認。 |
| 3 | degenerate denominators ガード | ✅ `R[i,j] = Va[i,j]/(E_i-E_j)` を `|E_i-E_j|>deg_tol` のみで計算。対角・縮退ペアは 0 → virtual sum (Va@R, R@Va) で `p=v,c` と「`p` が `n` または `m` と縮退」のケースを自動除外。出力でも `wcv > deg_tol` でマスク。NaN なし。 |
| 4 | 符号規約維持 (`σ ∝ +Σ Im[r_cv·r_vc;z]`, Im_mine = −Im_Fregoso) | ✅ `r_cv = Va[c,v].T/(i·wcv)` (textbook), `g = Im[r_cv * r_vc_a]`。テストで `g[0,0] == -D15` (rel<1e-7) を全 k で確認。σ 符号は D16 (負) と一致。 |
| 5 | Rice-Mele test 許容 | ✅ per-k 積分核: `rel=1e-7, abs=1e-14`。gap=2E: `abs=1e-12`。σ(ω): `n_kpts=6000, η=0.03`。これは「鉛筆と紙」レベルの精度。 |
| 6 | 多バンド builder の d²H/dk² = 解析的 | ✅ `d2Hdk_fn` は bond ブロックの位相 `e^{±i kd l}` の 2 階微分を解析的に (`(±i l)² = −l²`)。中心差分との一致もテスト。**数値微分ではない** → step size 依存性ゼロ。 |

**追加で確認:**

- **F4-3 vectorisation の正当性** — docstring の式変形:
  `Σ_{p≠v,c} v_vp v_pc (1/w_pc − 1/w_vp) = (Va@R)_vc − (R@Va)_vc − Δ·v/w_vc`
  これに `Va·Δ/w_vc` を足すと `2 Δv/w_vc + Σ`、すなわち Fregoso C2 のブラケット `[2vΔ/w − w_aa + Σ]` と一致。**算術的に正しい**。
- **`d2Hdk_fn(k, α)` シグネチャ** — 同一軸の 2 階微分のみ (σ_aaa 用)。σ_abc (異なる軸) は範囲外だが、F4-3 のスコープ的に妥当。今後 σ_xxz 等を欲しくなったら `d2Hdk_fn(k, α, β)` に拡張予定としてメモ。
- **`d2Hdk_fn` のキュービック多バンド限定** — 各 bond block が単一 k 成分にしか結合しないため、`d²/dk_α²` で他の axis 寄与は 0 で OK。Nestoklon の cubic Pm-3m / P4mm 構造前提なら正しい。RP 2D 等で異方性 bond を扱う際に再検証が必要。

## 3. F3 数値置換の妥当性

| δ (Å) | with-w (正) σ_zzz peak | no-w (旧 F3) peak | 倍率 |
|---|---|---|---|
| 0.10 | **−6.51e-01 @4.16 eV** | +2.68e-01 @3.70 eV | 2.43× |
| 0.15 | **−9.85e-01 @4.16 eV** | +4.09e-01 @3.70 eV | 2.41× |

→ 旧 F3 (`2147a3b`) は (a) 符号が逆、(b) magnitude 2.4× 過小、(c) ピーク位置 0.46 eV ずれ。**完全に置換が必要**で、報告書の通り破棄でよい。

**この発見の重要性:** Rice-Mele 2-band で per-k 積分核 = −D15 (rel<1e-7) という機械精度の一致は、符号・prefactor を closed form に anchor した状態。これは PRODUCTION_RULES が想定する「primary source validation」の教科書例。

## 4. Production-rules 違反チェック (PRODUCTION_RULES.md §8)

| 項目 | 結果 |
|---|---|
| 既存 production bundle | 2 件 (`theme_A_g_factor/2026-05-23_31374dc`, `phase_1.5_optical/2026-05-23_31374dc`) |
| MANIFEST.json 必須 8 フィールド (theme/git_commit/git_dirty/inputs/outputs/key_numbers/software/references) | 両方とも 21 keys、必須 8 揃い ✅ |
| `git_dirty: true` で commit されている bundle | なし ✅ (両方 False) |
| `convergence/` ディレクトリ | 両方存在 ✅ |
| `cowork/reports/` で shift-current の数値引用 | なし ✅ (F3-F4 は exploratory 段階、未昇格) |

**違反ゼロ。** F4-3 の発見は exploratory 段階で起き、Production 汚染を未然に防いだ — PRODUCTION_RULES の理想的な運用例。

## 5. 推奨ワークフロー（patrol 1655 §2.5 の再確認）

報告書の次タスク順序:
1. ✅ F4-3 修正実装 (本コミット)
2. **F4-2: Tan & Rappe 2016 (`references/pdfs/doi_10.1038_npjcompumats.2016.26.pdf`) でピーク位置・符号の多バンド sanity check** ← **次着手で OK**
3. F5: 9 材料 × δ スキャン (Production モード、F4-2 で符号確定後)

**承認します。** F4-3 で 2-band 内の sign/prefactor は closed form に anchor 済みなので、F4-2 は多バンド (CsPbI₃ 4-band) で
- ピーク位置 (band edge ω ~ 2 E_min 付近に来るか)
- 符号 (Tan & Rappe の図と qualitative に一致するか)
の 2 点を見れば十分。**省略しないこと**は §2.5 の指摘通り重要 — 多バンドの virtual sum 部の sign は 2-band では検出できない。

## 6. F4-2 着手前のお願い (任意)

F4-2 では多バンド `σ_zzz(ω)` を出します。比較対象 (Tan & Rappe Fig.) のピーク符号・位置と numbers を docstring か analysis note に明示しておくと、F5 promote 時に MANIFEST.references に書きやすくなります。Cowork 側で必要があれば Tan & Rappe PDF のテキスト化を手伝います。

---

## 7. 細かいスタイル提案 (どれも非必須)

1. `shift_current_integrand_aaa` の docstring に `r^a_{vc;a}` の式があるが、変数名 `wvc` (実数、符号付き = E_v - E_c < 0) と `wcv` の符号の取扱いがコード読みで一瞬迷う。`# wvc < 0 for occupied->unoccupied; wcv > 0` の一文があると親切。
2. `d2Hdk_fn: Callable | None = None` のデフォルトは「continuum 形 (WRONG for TB)」なので、明示パラメータ `require_w_term: bool = True` を追加して `None` のとき raise する案もある (defensive)。
3. `shift_current_zzz_abelian_deprecated` は名前通り deprecated。次の clean-up コミットで削除候補にしてよい (regression は新テストで担保されている)。

---

## 巡回結果 (2026-05-23 17:10 JST)
- 確認したファイル数: 4 (直近 30 分: F4-3 review request, F4-3 caught w-term, 自己発行 patrol ×2)
- レビュー依頼: **あり**(F4-3 fix commit `ffe2940`)→ 本ファイル §2-§5 で **APPROVE**
- BLOCKED: なし
- 最新コミット: `c25cca4` cowork/progress: F4-3 fix committed (5 分前) / 実体は `ffe2940`
- テスト独立再現: **20 passed (shift_current 系) / 225 passed (全 suite, 回帰ゼロ)**
- 進捗判定: **加速・健全** — F4-3 が 2-band Rice-Mele closed form に anchor され、physics が rock-solid に。次は F4-2 (多バンド sanity)
- 本番計算ルール違反: なし (PRODUCTION_RULES が想定する「exploratory で bug 捕捉」の教科書例)
- ユーザーへの通知: なし (Cowork 内で完結、F4-2 完了後にまとめて報告予定)
