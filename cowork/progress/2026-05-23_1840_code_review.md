# Code review: F5 design + `scan_shift_current_9materials.py` — 2026-05-23 18:40 JST

**Reviewer:** Cowork (scheduled supervisor)
**Target:**
- `cowork/progress/2026-05-23_1610_F5_design_review_request.md` (design, b2ccd8a)
- `scripts/scan_shift_current_9materials.py` (a70d9fc)
**Verdict:** **APPROVE design + 設計の物理は OK。ただし Production 実行前に script のバンドル化を要修正（§3 参照）。**

---

## 1. Design に対する判定（§7 の 3 質問への回答）

| Q | 回答 |
|---|---|
| Q1: Production 設定 **n_kpts=48 / η=0.10 / ω≤5 eV** で OK か | **✅ 承認**。η=0.10 は実測 η=0.08 (L2rel 1.9%) と η=0.15 (L2rel 0.9%) の中間で k 収束も保証されている。ω≤5 eV はピーク (~4 eV) を覆う適切な窓。 |
| Q2: δ ∈ {0.10, 0.15, 0.20} Å の 3 点で十分か | **✅ 承認**。線形性は F4 で確認済み（Rice-Mele 機械精度一致）、3 点あれば δ vs σ の傾き（=絶対スケール）を回帰できる。δ=0 のヌル（既に検証済）は scan に含めなくて良いが、`convergence/` か `logs/` に δ=0 → σ≈0 の値を 1 行残しておくと SI 引用時に強い。 |
| Q3: 絶対 μA/V² 換算を入れるか、相対のみで報告するか | **✅ §3 の方針で進めて OK**（相対を一次成果、絶対を桁・符号目安として併記、Blount 限界は MANIFEST.notes 明記）。CSV の絶対列は `_uA_per_V2_blount_limited` のように suffix で明示してほしい。reports/ で引用する数値は**相対値（または絶対値を caveat 付きで**）。 |

物理・手法・収束ロジックはすべてレビュー基準を満たしています。

---

## 2. 物理・手法面のチェック（OK）

- **一般化微分**: Fregoso 2017 Eq.(C2) TB 形（w 項込み）— F4-3 で Rice-Mele Eq.(D15)/(D16) と機械精度一致、F4-2 で多バンド対称則（Tan&Rappe Eq.14）一致。**符号・prefactor 確定済み**。
- **モデル**: Kashikar-13 の 13 軌道採用は shift-current 起源（s↔p spσ 符号交替, Tan&Rappe Eq.14）を陽に保持。4 軌道で X-p を畳み込むと失われる起源が保たれる。**理論的に正当**。
- **n_occ=20** = B-s² (2) + 3·X-p⁶ (18) で 9 材料共通。Kashikar 2021 の cubic CsBX₃ パラメータ系列で一貫している。
- **symmetry breaker**: [001] B-cation polar displacement + Harrison η=2.0 距離スケーリング。δ=0 で σ=0 を機械精度で確認済（F4 系列）。
- **single-thread BLAS** (script L24–27): `np` import 前に `os.environ.setdefault` で固定。**281s→5.9s 改善が再現できる**。重要な実装メモが冒頭 docstring にあるのも good。
- **References**: Fregoso 2017 PRB 96 075421 (Eq. C2/D12/D15) + Tan&Rappe 2016 npj CM 2:16026 + Young&Rappe 2012 PRL 109 116601 — 必要十分。Kashikar 2021 (TB モデル本体) も `MANIFEST.references` に含めること。

---

## 3. Production 実行前に script を修正してほしい点（**必須**）

現状 `scripts/scan_shift_current_9materials.py` は計算と CSV/PNG 出力までは揃っているが、**PRODUCTION_RULES §2–§4 のバンドル構造を生成しない**ため、このままだと Production 数値として引用できません。修正点:

### 3.1 出力ディレクトリ
- **現状**: `--outdir` デフォルト `results/shift_current/`
- **要修正**: Production 用に `results/production/theme_F_shift_current/2026-05-23_<git_short_hash>/` を自動生成（CLI で `--production` フラグを足す、または別 driver script を作る）。
- bundle ルート下に `inputs/`, `outputs/{raw,derived,figures}/`, `convergence/`, `logs/`, `README.md`, `MANIFEST.json` を必ず作る。

### 3.2 `MANIFEST.json` 生成（PRODUCTION_RULES §4 必須フィールド）
最低限以下を埋めること:
```
theme="theme_F_shift_current", subtask="F5 9-material scan",
date_utc, git_commit, git_branch, git_dirty,
physics_method="Fregoso2017 Eq.(C2) TB shift current, Kashikar-13 polar",
convergence={k_grid:[24,32,48], eta_eV:[0.05,0.08,0.15], omega_resolution:[150,300,600]},
inputs={config, parameters, cli, env, git_info, seeds},
outputs={csv, plots:[...], logs},
key_numbers={<material>_peak_sigma_zzz_rel_d0.15:..., <material>_peak_omega_eV:..., ×9 materials},
checksums_sha256={各 outputs ファイル},
software={python, numpy, scipy, matplotlib},
references=["Fregoso 2017 PRB 96 075421",
            "Tan & Rappe 2016 npj Comput Mater 2:16026",
            "Young & Rappe 2012 PRL 109 116601",
            "Kashikar et al. 2021 PRB 104 235203"],
notes="Relative units are the primary deliverable; absolute uA/V^2 is Blount-1962 limited (TB intra-atomic missing — same systematic as g-factor / f-sum). Single-thread BLAS forced before numpy import."
```

### 3.3 `inputs/` (PRODUCTION_RULES §3 必須)
- `inputs/scan_config.json`: `{n_kpts:48, eta:0.10, omega_min:0.3, omega_max:5.0, n_omega:300, deltas:[0.10,0.15,0.20], n_occ:20}`
- `inputs/kashikar13_params_9materials.json`: `data/parameters/kashikar2021_cubic_13orb.json` の subset（9 材料分）をコピー、または symlink/参照を MANIFEST に記録
- `inputs/materials.json`: 9 種一覧（B/X 組合せ）
- `inputs/cli.txt`: フル実行コマンド（`PYTHONPATH=src python scripts/... --production` の形）
- `inputs/env.yml` or `inputs/requirements.txt`: `pip freeze > inputs/requirements.txt`
- `inputs/git_info.txt`: `git log -1 --pretty=fuller`, `git branch --show-current`, `git status --short`
- `inputs/seeds.json`: `{"random_seeds": "not used (deterministic eigh + uniform k-grid)"}`

### 3.4 `git_dirty` の事前チェック（**必須**）
script 冒頭で `git status --short` を実行し、空でなければ stderr に warning + `--allow-dirty` フラグなしでは exit。`MANIFEST.git_dirty` には `subprocess` の結果を入れる（patrol 1740 で違反ゼロ継続中、今回も維持してほしい）。

### 3.5 `convergence/` 配置
script の `convergence` モード出力は現在 `{outdir}/convergence/` だが、Production 実行時は bundle 内の `convergence/` に行くべき。**収束プローブと本番 scan は同じ commit hash でバンドルしてほしい**（patrol 1725 §6-5）。`run` 一本でその両方を順に実行する driver があると良い。

### 3.6 `README.md`（150 字程度）
PRODUCTION_RULES §2.1 で必須。実行内容と Reproduction コマンドを書く。

---

## 4. Minor / Nice-to-have

- script L127 の `__import__("perovskite_tb.models_kashikar", fromlist=...)` は **冒頭 import に格上げ**してほしい（PEP 8 違反、レビュアビリティ低下）。
- `peak_summary.csv` は良い idea。`peak_omega_d{d}` の単位は header に `_eV` を足すと一目で分かる。
- L132 の `peak` 計算は `argmax(|s|)` で sign を残しているのが良い（shift current は符号が物理的に意味を持つため）。
- `gaps[mat]` は R-point の N_OCC 直前/直後 eigenvalue 差を取っているが、R は CBM/VBM ではない材料がある（特に Ge 系で Γ-X や R-M）。 `gap` は **CBM-VBM の正しい定義（BZ 上 min）** に置き換える OR header を `gap_at_R_eV` のように R 点限定であることを明示してほしい。
- `colors = {"Ge":green, "Sn":blue, "Pb":red}` は colorblind-safe ではない。SI 図用に viridis 系 + 明示 marker を別変種で用意するなら歓迎（必須ではない）。
- 9 材料 × 3 δ ≈ 27 call ≈ 16 分（design §6 の見積り）の **計測ログを `logs/run.log` に残す**こと（runtime_seconds を MANIFEST に書くため）。

---

## 5. 進行可否

- **設計レビュー: ✅ PASS**（物理・収束・参照は十分）
- **Script 単体: ⚠️ Exploratory モードまでは OK、Production には §3 のバンドル化が必要**
- **次のアクション（推奨順）**:
  1. §3.1–3.6 を実装した `--production` モードを script に追加（または `run_production.sh` ラッパー）
  2. `git status --short` クリーン状態で `convergence` → `scan` を順に実行
  3. `MANIFEST.json` + `README.md` + checksums を生成し commit
  4. 次の `cowork/progress/.../F5_production_done.md` で報告 → F6 報告書（`cowork/reports/theme_F_shift_current.md`）へ
- §3 の修正なしで Production 投入してしまった場合は **`exploratory` タグ付きで再実行**になります（前例: Phase 1.5 optical の MANIFEST 整備）。最初から bundle 生成しておくのが省力。

---

## 6. 物理的に確認したい未解決点（Production 結果が出てから議論）

主問い「鉛フリー（Sn/Ge）で σ が大きい組成があるか」に答えるには:
- σ_zzz の **ピーク強度** だけでなく **ω 積分（[E_g, 2E_g] 帯）** や **bandgap-normalized σ** も見たい。
- Sn/Ge は spin-orbit が Pb より弱い → SOC 効果（F-D8 で予定の SOC on/off 比較）の感度が違う。
- Cs₂AgBiBr₆ 等のダブルペロブスカイトとの比較は F5+ の射程外で良い（research_ideas に残しておく）。

これらは F5 結果が出てから F6 報告書で議論ポイントとして書けば十分です。

---

## 巡回結果 (2026-05-23 18:40 JST)
- 確認したファイル数: 3（design.md, script, 直近 patrol/clarification）
- レビュー依頼: **あり → 本ファイルで回答**
- BLOCKED: なし
- 最新コミット: `a70d9fc` F5: add scan_shift_current_9materials.py (15:35 UTC, 2 分前)
- 進捗判定: **順調** — 1825 patrol の「3 連続静穏」状態を脱出、design + script が連続 commit で提出された
- 本番計算ルール違反: なし（ただし §3 を実装してから Production 実行へ）
- ユーザーへの通知: なし
- 未着手リマインド: 1410 directive (D ドライブ classic 15 件、6 連続未着手) — F5 Production の長時間計算中に並行で取り組む良いタイミング
- 次のエスカレーション閾値: 1855 patrol で §3 修正に着手していなければ status ping、または `--allow-dirty` 等の例外要請があれば判断
