# Code Review Round 7 — Cowork → Claude Code 03c7c1c (R4-STD-2/3 + R5-COW-1 close)

**発行元:** Cowork (scheduled-task supervisor, 巡回 1540 JST = 04:50 UTC)
**対象 commit:** `03c7c1c R4-STD-2/3 (claude code): centralize HBAR2_OVER_M0 in _constants.py + g_factor docstring citation anchor`
**対象進捗ファイル:** `2026-05-24_1520_r5cow1_resolve_and_r4std_claude_code.md`
**判定:** ✅ **承認（標準 6 観点すべて合格・回帰ゼロ）**

## 1. 検証方法（mount-safe）

R5-COW-1 で確認した Cowork sandbox mount stale 問題（`git status` で `fatal: unknown index entry format 0xd5fc0000` 再発、disk wc が HEAD と乖離）を回避するため、**全検証を `git archive HEAD` 経由で /tmp に clean snapshot を展開してから実行**:

```
WORKDIR=/tmp/tbhead_1779598387
git archive --format=tar HEAD src/ tests/ data/ pyproject.toml | tar -xf - -C $WORKDIR
pip install -e $WORKDIR --break-system-packages
```

これにより stale mount に依存せず、HEAD 内容に対して直接 import / pytest / object identity 検証を実施。

## 2. 標準 6 観点判定

| # | 観点 | Round 7 判定 | 根拠 |
|---|---|---|---|
| 1 | 物理的正しさ | ✅ 合格 | 定数値 `HBAR2_OVER_M0=7.619964 eV·Å²` 不変、Roth-Lax-Zwerdling 1959 (PR 114, 90) 引用は module docstring (g_factor.py L4) と完全一致 |
| 2 | 検証の十分性 | ✅ 合格 | pytest 231 passed in 20.27s（fresh HEAD snapshot で再現確認） |
| 3 | 既存テスト破壊 | ✅ なし | 231/231（前: 231/231） |
| 4 | コード品質 | ✅ 合格 | DRY 達成。R4-STD-1 (type hints) は deferred 妥当 |
| 5 | ハルシネーション防止 | ✅ 強化 | `compute_g_factor` docstring に Roth-Lax-Zwerdling PR 114, 90 anchor 追加（モジュール docstring 既出の出典のみ使用） |
| 6 | 本番計算ルール | ✅ 違反なし | 6 MANIFEST 不変、git_dirty=False、key_numbers 計 159 維持、convergence 完備 |

## 3. R4-STD-2 (HBAR2_OVER_M0 centralization) の独立検証

```python
from perovskite_tb._constants import HBAR2_OVER_M0
from perovskite_tb import exciton, optical, velocity, g_factor
assert exciton.HBAR2_OVER_M0 is HBAR2_OVER_M0       # ✅
assert optical.HBAR2_OVER_M0  is HBAR2_OVER_M0      # ✅
assert velocity.HBAR2_OVER_M0 is HBAR2_OVER_M0      # ✅
assert g_factor.C_HBAR2_OVER_M0 is HBAR2_OVER_M0   # ✅ (alias preserves public name)
```

**結果: 4 モジュールすべて同一 Python オブジェクト**（`is` 比較で True）— 真の single source of truth を達成。値再代入ではなく import なので CODATA 更新時の伝播も `_constants.py` 1 箇所で完結。

`scripts/plot_ge_universality.py` は `from perovskite_tb.g_factor import C_HBAR2_OVER_M0 as C` を使用しており、g_factor が alias を re-export するため **import 互換性 100%**。`velocity.py` の `noqa: F401` re-export も適切（外部からの後方互換 `from perovskite_tb.velocity import HBAR2_OVER_M0` 維持）。

### 軽微な所見（行動不要）

- `scripts/plot_theme_A_figures.py:32` に `C = 7.619964` がローカル定数として残存。R4-STD-2 の original scope は src/ 4 モジュールのみで、この plot script は対象外。将来の cleanup 候補だが、`scripts/` は Production 計算経路に乗らない可視化補助なので **本ラウンドでは指摘しない**。
- `cowork/reports/theme_I_exciton.md:22` の本文にも `7.619964 eV·Å²` がリテラル記述あり。これは physics narrative であり、定数の重複定義ではない。

## 4. R4-STD-3 (citation anchor) の独立検証

`src/perovskite_tb/g_factor.py` L97-100:

```python
"""Roth-Lax g-tensor for the Kramers doublet starting at ``band_index``.

Implements the effective g-factor of Roth, Lax & Zwerdling, Phys. Rev. 114,
90 (1959) (the ``M_gamma`` formula in the module docstring): the band-edge
Zeeman matrix is the spin term ``g0*sigma`` plus the interband (velocity)
orbital sum ``dG``.
```

- モジュール docstring (L4): `L. M. Roth, B. Lax, S. Zwerdling, Phys. Rev. 114, 90 (1959)` と完全一致 ✅
- `M_gamma` formula reference (L13-15) を function docstring から明示参照 ✅
- 既出の出典のみ使用（追加の未検証引用なし）→ ハルシネーション防止強化 ✅

## 5. R5-COW-1 close の独立検証

Claude Code 1520 報告:
> 私の環境で厳密検証: git status 正常、git diff HEAD 空、各 report の git show HEAD vs working tree バイト数全て一致

私の bash sandbox では依然として:
- `git status` → `fatal: unknown index entry format 0xd5fc0000`（再発）
- `git diff HEAD --stat` → 多数ファイルの偽差分（disk が HEAD より古い）
- `src/perovskite_tb/exciton.py` を直接 import 試行 → `SyntaxError: unterminated triple-quoted string literal` (line 77; HEAD 内容は 84 行で完全)

➡️ **Cowork sandbox の mount は確実に stale snapshot**。Claude Code が報告した「ユーザー local repo は健全」は私の側からは直接検証できないが、`git show HEAD:` 経由で参照可能なコンテンツが完全（バイト数・syntax 両面）であり、HEAD は健全。

➡️ **PI が local で `git status && git diff HEAD --stat` が clean なら R5-COW-1 は close**。Cowork sandbox の infrastructure 問題のみが残り、これは Anthropic 側の課題（私からは fix できない）。

## 6. Production 数値への影響評価

定数値が文字通り不変（7.619964 → 7.619964）なので、以下すべて **rerun 不要**:

| Production bundle | 依存 | 影響 |
|---|---|---|
| theme_I_effective_mass | `m*/m0 = HBAR2_OVER_M0 / d²E/dk²` | ✅ ゼロ |
| theme_I_exciton (+ MP_DFPT) | `μ = HBAR2_OVER_M0 / d²E/dk²` 由来 | ✅ ゼロ |
| theme_A_g_factor | Roth-Lax で `C = HBAR2_OVER_M0` | ✅ ゼロ |
| phase_1.5_optical | Drude/Kane で `HBAR2_OVER_M0` | ✅ ゼロ |
| theme_F_shift_current | velocity ops 経由（HBAR2_OVER_M0 直接使用なし） | ✅ ゼロ |

6 MANIFEST すべて `git_dirty: False`、key_numbers 計 159 不変、convergence 完備。

## 7. レビューサイクル進捗

| Round | Cowork 指摘 | Claude Code 指摘 | 連続ゼロ |
|---|---|---|---|
| 1 | 17 | 4 (unique 16) | 0 |
| 2 | 1 | 8 (R6-10 + R2-2/3/4 web) | 0 |
| 3 | 4 | 1 | 0 |
| 3-DEEP | 6 | — | 0 |
| 4 | — | 0 | (片側 0) |
| 5 | 1 (infrastructure) | 5 | 0 |
| 6 | 0 | — | (片側 0) |
| **7** | **0** | **(自己レビュー次回, R4-STD 実装は self-tracked)** | **1（双方ゼロ初到達相当）** |

合格条件「連続 3 ラウンド両者ゼロ」まで残り 2 ラウンド だが、PI 判断で **論文化に進む** ことで cycle 終了も妥当（narrative 完璧主義 vs 実用優先のトレードオフ; Cowork 1445 §8 推奨と一致）。

## 8. 新規指摘（Round 7 Cowork）

**なし**。03c7c1c は R4-STD-2/3 を正しく実装し、テスト・provenance・citation すべて健全。

## 9. PI 通知（このセッションで報告済）

ユーザー（PI）から「プロジェクトが収束したらパトロール止めていいよ。トークン消費を抑えたい」との指示あり (1540 JST 受領)。

**Cowork 判定:** プロジェクトは narrative + code 両面で収束:
- narrative: Round 6 で新規ゼロ
- code: Round 7 (本 commit) で R4-STD-2/3 解決 + テスト 231/231
- Production: 6 bundle すべて健全、追加計算不要
- 残作業: PDF backlog 3 件（Yang 2017 / CsPbCl₃ 64–77 meV / Blount 1962 OA 確認）と R4-STD-1 (type hints) のみ。これらは論文化と並行可能。

**`tb-perovskite-supervisor` scheduled task を本ラウンドをもって disable します**。再開が必要になれば PI が手動で enable してください（`tb-perovskite-novelty-watch` は週 2 回の軽量タスクなので継続）。

## 10. 巡回結果サマリ

```
## 巡回結果 (2026-05-24 15:40 JST)
- 確認したファイル数: 1 (新規 Claude Code 1520) + commit 03c7c1c (12 ファイル)
- レビュー依頼: 1 件（src 変更の標準 6 観点再検証）→ 本 1540 で対応
- BLOCKED: なし
- 最新コミット: 03c7c1c R4-STD-2/3 (claude code): centralize HBAR2_OVER_M0 in _constants.py + g_factor docstring citation anchor
- 進捗判定: 順調・収束（narrative + code 両面）
- 本番計算ルール違反: なし（6 MANIFEST 不変、key_numbers 159 維持）
- ユーザーへの通知: あり（収束判定済 → scheduled task disable 実施予定）
```
